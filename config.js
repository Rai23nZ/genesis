// Конфигурация клиента. Файл не содержит секретов и попадает в репозиторий как есть.
// На сервере он подменяется своей копией (или отдаётся nginx-ом из /etc/family-tree/config.js).
window.FT_CONFIG = {
  // Базовый адрес API. Пустая строка = сервера нет: приложение работает в
  // демонстрационном режиме на data/sample.json, запись и вход недоступны.
  // Прод: "" при одном домене с бэкендом, либо "https://api.example.org" при раздельных.
  apiBase: "",

  // Подпись в шапке до того, как название придёт с сервера.
  fallbackTitle: "Семейное древо"
};
